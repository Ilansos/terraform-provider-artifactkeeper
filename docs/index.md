# Artifact Keeper Provider

The Artifact Keeper provider manages repositories, users, groups, permissions, webhooks, lifecycle policies, security policies, and license policies through the Artifact Keeper REST API.

Use `url` or `base_url` plus either `token` or `username` and `password`. The provider also supports the `ARTIFACTKEEPER_*` environment variables documented in the README.
